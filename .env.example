AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
APP_BASE_URL=http://localhost:3000
NEXT_PUBLIC_AUTH0_CONNECTION_GOOGLE=google-oauth2
# Optional: path to a service-account JSON. If unset, ADC falls back to
# `gcloud auth application-default login` user creds (recommended for local dev).
# GOOGLE_APPLICATION_CREDENTIALS=./gcp-firestore-sa.json
FIRESTORE_PROJECT_ID=cadre-test-mvp-260522
